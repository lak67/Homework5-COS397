name: Sort Lib

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      
jobs:
  linting:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: [3.9, 3.10]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install tools
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt
    - name: Lint with black
      run: |
        python -m black --check --diff .
    - name: Lint with flake8
      run: |
        python -m flake8 . --show-source --statistics


  test:
    # only run the code testing if linting passed
    needs: linting
    strategy:
	  matrix:
        python-version: [3.9, 3.10]
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt
    - name: Test with pytest
      run: |
        python -m pytest


  packaging:
    # only run the packaging if testing passed
    needs: test
    strategy:
      matrix:
        python-version: [3.9, 3.10]
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install -r requirements-dev.txt
    - name: Build package
      run: |
        python -m build
        # see that the dist directory contains the wheel
        ls -la dist
    - name: Upload package
      if: github.event_name == 'push'
      # uses: actions/upload-artifact@v3
      # with:
      #   name: python-packages ${{ matrix.os }} ${{ matrix.python-version }}
      #   path: dist
      # run:
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: pypi-AgENdGVzdC5weXBpLm9yZwIkNmJjMjI5MGItYzk1NS00NWNkLWJlMzUtYWFkNmYxZWIwMzEyAAIqWzMsIjVmN2ZjMTE4LTIwY2QtNDYwMS04ODE3LTRiZGI2MGRkYjNmOSJdAAAGIKRL6lTaSOXBQW8C_xfERe1zaK_V2Hqt4ZAhRKDkojFN